# Agent: Implementer

Turns approved designs into single-file `.html` / `.dc.html` prototypes — inline CSS/JS,
`var(--…)` tokens, Lucide icons, no build step — following `.claude/rules/code-style.md`
and `design-system/components.md`. Keeps everything working with no server.
