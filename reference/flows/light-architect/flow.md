# Light ARchitect — reading flow (case study 06)

Image-led narrative plus three working in-page simulations on the deep dive.

**Entry:** homepage work rows (06) / the homepage Light ARchitect scene's "Read the case study"
pill (promoted from "coming soon") / work index & lobby tile / concierge card (Quick tour · Deep
dive · Try the mini planner) / case-study switcher on any sibling page / search.

**Quick tour (light-architect-showcase.html):** hero on a real satellite design → the
specialist-bottleneck problem → the design surface (boundaries, measure) → defensible photometrics
(summary, advanced calc) → IES library → sports templates → AR origins + photo mode →
honest numbers → patent strip → CORE hand-off. Exit: deep-dive CTA, next-case footer
(→ Enterprise AI), switcher.

**Deep dive (light-architect.html):** 11 TOC-driven sections; the three interactives are the
spine — 06 photometric sandbox (drag/keyboard poles vs the basic parking target), 07 IES filter-before-load demo,
08 AI AutoLayout two-stage loop (boundary → goals → staged progress → ranked candidates → refine →
re-rank). Every simulation carries an honesty label; the shipped product's screenshots sit beside
each demo as the receipt. Exit: next-case footer (→ Enterprise AI), switcher, work index.

**Failure/edge states:** widgets init isolated (one failure can't kill the page); keyboard paths
on all three interactives (roving tabindex, arrow-key movement, Delete); reduced-motion renders
progress/counters instantly with no autonomous animation; images lazy-load with factual alt text;
no external embeds — fully offline-capable.
